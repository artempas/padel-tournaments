/**
 * Реквизиты политики обработки персональных данных.
 *
 * Отдельным модулем, а не константами внутри страницы, потому что оператор и
 * адрес для обращений задаются окружением: приложение ставят у себя, и владелец
 * копии — не владелец этого репозитория. Значения по умолчанию описывают
 * основную установку.
 *
 * Модуль серверный: `NEXT_PUBLIC_` в именах намеренно нет. Такие переменные
 * Next вшивает в бандл на сборке, а образ здесь собирается один раз и получает
 * настройки при запуске — вшитым оказалось бы пустое значение. Поэтому читает
 * их только серверный рендер страницы, и та объявлена динамической.
 */

/**
 * Кто обрабатывает данные. По закону нужны полное имя (или наименование
 * организации) — задайте PRIVACY_OPERATOR, значение ниже лишь заглушка.
 */
export const OPERATOR = process.env.PRIVACY_OPERATOR?.trim() || 'Артём';

/** Куда писать по вопросам данных: отзыв согласия, удаление, копия. */
export const CONTACT_EMAIL = process.env.PRIVACY_EMAIL?.trim() || 'artem1459@gmail.com';

/** Дата редакции. Меняется вместе с текстом политики — и только вместе с ним. */
export const REVISED_AT = '2 августа 2026 г.';
